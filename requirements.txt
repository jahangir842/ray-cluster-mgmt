# LLM & Transformers
transformers>=4.40.0
accelerate>=0.30.0
huggingface_hub
vllm==0.6.6
# vLLM guided decoding — outlines replaces xgrammar as the backend due to a
# TokenizerInfo.from_huggingface bug in xgrammar 0.2.x (see docs/troubleshooting-vllm.md)
outlines

# Distributed & Performance
tensorboard
sentencepiece
protobuf
mlflow
matplotlib

# Ray Serve LLM (examples/7-ray-serve) — ray.llm._internal unconditionally
# imports boto3 for its cloud model-mirroring path even when unused, so it's
# a hard import-time dependency, not optional. Install `ray[serve,llm]`
# itself separately (see CLAUDE.md), PINNED to 2.48.0 — newer Ray releases
# restructured ray.llm._internal to import vllm.entrypoints.openai.chat_completion,
# which doesn't exist in vllm==0.6.6 (pinned above) and crashes at import
# time. Ray 2.48.0 vendors its own OpenAI protocol models instead and has no
# such dependency; confirmed working against this repo's serve_vllm.py.
boto3
